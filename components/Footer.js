import css from '../styles/footer.module.scss'

export default function Footer() {
    return(
        <>
            <footer className={css.mainFooter}>
                <h2 className={css.footerTitle}>Footer</h2>
            </footer>
        </>
    )
}
